{
  "name": "@code-charter/vscode",
  "displayName": "Code Charter",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.87.0"
  },
  "publisher": "crjfisher",
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:code-charter-vscode.generateDiagram"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "code-charter-vscode.generateDiagram",
        "title": "Code Charter: Visualize Code Trees"
      }
    ],
    "configuration": {
      "title": "Code Charter",
      "properties": {
        "code-charter-vscode.clusteringAlgorithm": {
          "order": 1,
          "type": "string",
          "default": "spectral",
          "enum": [
            "spectral",
            "kmeans",
            "agglomerative"
          ],
          "enumDescriptions": [
            "Spectral clustering: uses graph Laplacian eigenvalues. Best for code dependency graphs with non-convex cluster shapes.",
            "K-means: fast partitioning by minimizing within-cluster variance. Good for roughly spherical clusters.",
            "Agglomerative: bottom-up hierarchical merging. Good for discovering nested code module hierarchies."
          ],
          "description": "Clustering algorithm used to group related code symbols."
        },
        "code-charter-vscode.clusteringMaxClusters": {
          "order": 2,
          "type": "number",
          "default": 12,
          "minimum": 2,
          "maximum": 30,
          "description": "Maximum number of clusters. The algorithm searches for the optimal count up to this limit."
        },
        "code-charter-vscode.devMode": {
          "order": 3,
          "type": "boolean",
          "default": false,
          "description": "Enable development mode to load UI from dev server"
        },
        "code-charter-vscode.devServerUrl": {
          "order": 4,
          "type": "string",
          "default": "http://localhost:3000",
          "description": "URL of the UI development server"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "tsc -p ./",
    "compile": "tsc -p ./",
    "dev": "tsc -watch -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf out"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^18.19.26",
    "@types/vscode": "^1.87.0",
    "@typescript-eslint/eslint-plugin": "^5.9.1",
    "@typescript-eslint/parser": "^5.62.0",
    "@vscode/test-cli": "^0.0.6",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@ariadnejs/core": "^0.8.0",
    "@ariadnejs/types": "^0.8.0",
    "@code-charter/types": "^0.0.1",
    "@code-charter/ui": "^0.0.1",
    "@huggingface/transformers": "^3.7.1",
    "@tensorflow/tfjs-node": "^4.10.0",
    "@vscode/python-extension": "^1.0.5",
    "clustering-tfjs": "^0.5.0"
  }
}
